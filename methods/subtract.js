/**
 * Subtracts `a` from `b`
 *
 * @static
 * @memberOf _test
 * @since 1.0.0
 * @category Math
 * @example
 * 
 * console.log(subtract(10, 7))
 * // => 3
 *
 */
function subtract(a, b) {
    return a - b
}
module.exports = subtract
